√енераци€ формы (GET/users/new)

—оздадим инстанс нашей предварительно определенной формы:
user_form = UserForm()
type(user_form)

__main__.UserForm

  каждому полю формы мы можем обратитьс€ отдельно по ее атрибуту:
type(form.first_name)
	wtforms.fields.core.StringField

¬ самом простом случае то все. “еперь инстанс нашей формы можно передать шаблонизатору
дл€ отображени€:

def new(self, request):
	user_form = UserForm()
	
	render('new_uer.html', {
		'form': user_form,
	})

ћетод render, конечно, специфичен. ¬ твоем случае методы рендеринга будут определ€тьс€
фреймворком и шаблонизатором, который ты используешь.

ќтлично, передали нашу форму в шаблонизатор.  ак ее отрендерить в шаблоне?
ѕроще простого. Ќапомню, что мы рассматриваем процесс на примере Jinja2.

{{ form.first_name.label}}
{% if form.first_name.errors %}
	<ul class = "errors">
		{% for error in form.first_name.errors %}
			<li>{{error}}</li>
		{% endfor %}
	</ul>
{% enfif %}
{{ form.first_name() }}

 од выше с user_form в качестве form будет преобразован шаблонизатором в следующую разметку.
<label for="first_name">First name</label>

<input id="first_name" name="first_name" type="text" value="">

«десь происходит вот что.
1. ¬ первой строке мы обратились к атрибуту label пол€ first_name нашей формы.
¬ нем содержитс€ HTML-код лейбла нашего пол€ first_name. “екст беретс€ из описани€ класса 
формы из соответствующего атрибута пол€.

2. «атем мы проверили содержимое списка errors нашего пол€.  ак нетрудно догадатьс€, в нем 
содержатс€ ошибки. Ќа данный момент ошибок в нем нет, поэтому блок не вывел ничего. 
ќднако если бы эта форма уже заполн€лась и была заполнена (например, валидатор от 6 до 30
по длине не пропустил значение), то в список пол€ попала бы эта ошибка. ћы увидим работу
валидаторов дальше.

3. » наконец, в последней строке мы рендерим сам тег input, вызыва€ метод .first_name()
нашего инстанса формы.

¬се очень гибко. ћы можем рендерить все атрибуты пол€ или только сам тег input.
Ќетрудно догадатьс€, что теперь мы можем сделать то же самое и дл€ всех остальных полей,
отрендерив все пол€ формы или только их часть соответствующими им встроенными 
HTML-виджетами.